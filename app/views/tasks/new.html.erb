<%= simple_form_for @task, url:tasks_create_path do |f| %>
  <%= f.input :title %>
  <%= f.input :memo %>
  <%= f.select :data, Task::SELECT_DATA, {}, {:multiple => true} %>
  <%= f.button :submit %>
<% end %>
