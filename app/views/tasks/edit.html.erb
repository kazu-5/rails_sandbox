<%= simple_form_for @task do |f| %>
  <%= f.input :id, :as => :hidden %>
  <%= f.input :title %>
  <%= f.input :memo %>
  <%= f.select :data, Task::SELECT_DATA, {:selected => @task.data}, {:multiple => true} %>
  <%= f.button :submit %>
<% end %>

